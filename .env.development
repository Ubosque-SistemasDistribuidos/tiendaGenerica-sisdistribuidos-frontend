# Modo Mock (datos locales en localStorage)
# Cambiar a 'false' para usar API real
VITE_USE_MOCK_BACKEND=true

# URL del backend real (usado cuando VITE_USE_MOCK_BACKEND=false)
VITE_API_URL=http://localhost:3000/api
